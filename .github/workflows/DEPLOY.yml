name: DEPLOY

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - name: Docker Login
      uses: azure/docker-login@v1
      with:
        login-server: docker.pkg.github.com
        username: yurii.myslyvets@gmail.com
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Start App
      run: |
        docker-compose up -d
        docker-compose down
